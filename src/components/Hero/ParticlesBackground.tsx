'use client'

// This component is no longer used in the new landing page design.
// Kept as a placeholder to avoid import errors from other files.
export default function ParticlesBackground() {
    return null
}
